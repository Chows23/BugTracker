@model List<Bug_Tracker.Models.ProjectUser>

@{
    ViewBag.Title = "Dashboard";
}

<h2>Dashboard</h2>

@if (Model.Count == 0)
{
    <h3>Nothing to display.</h3>
}
else
{
    <ul>
        @foreach (var item in Model)
        {
            <li>@item.Project.Name</li>
        }
    </ul>
}